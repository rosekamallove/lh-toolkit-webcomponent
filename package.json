{
  "private": true,
  "name": "lh-toolkit-webcomponents",
  "description": "The FHIR webcomponents based on HTML custom elements",
  "license": "MPL-2.0",
  "scripts": {
    "start": "polymer serve",
    "build": "polymer build",
    "lint": "polymer lint",
    "test": "polymer test",
    "test:integration": "polymer build # test that psk builds without error with the CLI"
  },
  "devDependencies": {
    "async": "^1.5.2",
    "lodash": "^3.10.1",
    "polymer-cli": "^1.7.1",
    "wct-browser-legacy": "^1.0.1"
  },
  "engines": {
    "node": ">=8.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "lit-element": "^2.3.1"
  }
}
